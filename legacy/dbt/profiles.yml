default:
  outputs:
    dev:
      type: duckdb
      path: "{{ env_var('DATABASE_PATH', '../data/database.duckdb') }}"
      threads: 1
      settings:
        preserve_insertion_order: false
  target: dev
